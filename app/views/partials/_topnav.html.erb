<!-- top navigation -->
<div class="top_nav">

  <div class="nav_menu">
    <nav class="" role="navigation">
      <div class="nav toggle">
        <a id="menu_toggle"><i class="fa fa-bars"></i></a>
      </div>

      <ul class="nav navbar-nav navbar-right">
        <li class="">
          <a href="javascript:;" class="user-profile dropdown-toggle" data-toggle="dropdown" aria-expanded="false">

            <% if current_user.present? %>
              <% if current_user.resumes.present? %>
                <%= image_tag current_user.resumes.last.header.avatar.url %>
                <%=current_user.user_name.titleize%>
              <% else %>
                <%= image_tag "default_avatar.png"%>Guest
              <% end %>    
            <% else %>
                <%= image_tag "default_avatar.png"%>Guest
            <% end %>
            <span class=" fa fa-angle-down"></span>
          </a>
          <ul class="dropdown-menu dropdown-usermenu pull-right">
            <% if current_user.user_type.eql?("guest") %>
              <li><a href="javascript:;" onclick="check_user_type()" ><i class="fa fa-user-plus  pull-right"></i> Login/Sign Up</a>
              </li>
            <% else %>
              <li><a href="/basic_info/<%=current_user.id%>/edit" ><i class="fa fa-pencil-square-o  pull-right"></i> Edit Profile</a>
              <li><a href="/basic_info/<%=current_user.id%>/update_password" ><i class="fa fa-key pull-right"></i> Change Password</a>
              <li><a href="/resumes" ><i class="fa fa-database pull-right"></i> Your Resumes</a>
              </li>
              <li><a href="/users/sign_out" data-method="delete"><i class="fa fa-sign-out pull-right"></i> Log Out</a>
              </li>  
            <% end %>
          </ul>
        </li>
      </ul>
    </nav>
  </div>

</div>
<!-- /top navigation -->
<script type="text/javascript">
  function check_user_type() {
    $(".cd-signin").click();
  }
</script>